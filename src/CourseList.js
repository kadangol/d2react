function CourseList(props) {
    return (<div>
        <h5> {props.courseCode} </h5>
        <h5> {props.courseName} </h5>
        <h4> === === === === === === === === === === === == </h4>
    </div>

    )
};

export default CourseList;